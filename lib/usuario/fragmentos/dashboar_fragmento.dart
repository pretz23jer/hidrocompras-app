import 'package:flutter/material.dart';

class DashboardFragmentos extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Tablero"),
      ),
    );
  }
}
